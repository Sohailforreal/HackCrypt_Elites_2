# System Architecture

## 🏗️ High-Level Architecture

```
┌─────────────────────────────────────────────────────────────────┐
│                         USER INTERFACE (React)                   │
├─────────────────────────────────────────────────────────────────┤
│  ┌──────────────────┐  ┌──────────────────┐  ┌──────────────────┐ │
│  │   Registration   │  │  Mark Attendance │  │    Dashboard     │ │
│  │      Form        │  │   (MFA Flow)     │  │   Analytics      │ │
│  └──────────────────┘  └──────────────────┘  └──────────────────┘ │
│         │                      │                      │           │
│         └──────────────────────┴──────────────────────┘           │
│                          │                                         │
└──────────────────────────┼─────────────────────────────────────────┘
                           │
                 ┌─────────▼─────────┐
                 │    API Client     │
                 │   (axios/fetch)   │
                 └─────────┬─────────┘
                           │
                  ┌────────▼────────┐
                  │  HTTP/REST API  │
                  │  (Port 5000)    │
                  └────────┬────────┘
                           │
┌──────────────────────────┼──────────────────────────────────────────┐
│                   EXPRESS.JS BACKEND                                │
├──────────────────────────┼──────────────────────────────────────────┤
│                          │                                           │
│  ┌──────────────────────▼─────────────────────┐                    │
│  │         Route Handlers                     │                    │
│  ├──────────────────────────────────────────┤                    │
│  │ • POST /students/register                │                    │
│  │ • GET /students/all                      │                    │
│  │ • GET /students/:id                      │                    │
│  │ • POST /students/biometric/:id           │                    │
│  │ • POST /attendance/verify                │                    │
│  │ • GET /attendance/student/:id            │                    │
│  │ • GET /attendance (with filters)         │                    │
│  └──────────────────────────────────────────┘                    │
│                          │                                         │
│  ┌──────────────────────▼─────────────────────┐                    │
│  │     Business Logic & Utilities             │                    │
│  ├──────────────────────────────────────────┤                    │
│  │ • QR Code Generator                      │                    │
│  │ • Biometric Verifier                     │                    │
│  │ • Multi-Factor Authentication            │                    │
│  │ • Data Validation                        │                    │
│  │ • Error Handling                         │                    │
│  └──────────────────────────────────────────┘                    │
│                          │                                         │
└──────────────────────────┼──────────────────────────────────────────┘
                           │
                  ┌────────▼─────────┐
                  │   MONGODB        │
                  │   Database       │
                  │  (Port 27017)    │
                  └──────────────────┘
```

---

## 🔄 Data Flow - Registration Process

```
┌─────────────────────┐
│   Student Form      │
└──────────┬──────────┘
           │
           │ Name, StudentID, Class, Division, Gender
           ▼
┌──────────────────────────────┐
│   Validation Check           │
│  • All fields filled?        │
│  • StudentID unique?         │
└──────────┬───────────────────┘
           │
           ▼
┌──────────────────────────────┐
│   QR Code Generation         │
│  • Combine student data      │
│  • Generate QR image         │
│  • Convert to base64         │
└──────────┬───────────────────┘
           │
           ▼
┌──────────────────────────────┐
│   Save to MongoDB            │
│  • Store student info        │
│  • Store QR code             │
│  • Set timestamps            │
└──────────┬───────────────────┘
           │
           ▼
┌──────────────────────────────┐
│   Return Response            │
│  • Success message           │
│  • QR code to display        │
│  • Student ID for biometric  │
└──────────────────────────────┘
```

---

## 🔐 Data Flow - Multi-Factor Attendance

```
┌─────────────────────┐
│   QR Code Scan      │
│   ↓ Returns         │
│   Student Data      │
└──────────┬──────────┘
           │
           ▼
┌──────────────────────────────┐
│   Fingerprint Capture        │
│  ↓ Returns                   │
│   Fingerprint Hash           │
└──────────┬───────────────────┘
           │
           ▼
┌──────────────────────────────┐
│   Photo Capture              │
│  ↓ Returns                   │
│   Face Descriptor            │
└──────────┬───────────────────┘
           │
           ▼
┌──────────────────────────────┐
│   Verification Engine        │
└──────────┬───────────────────┘
           │
    ┌──────┴──────┐
    │             │
    ▼             ▼
 ┌──────┐     ┌──────────────┐
 │ QR   │     │ Fingerprint  │
 │Check │     │ Verification │
 └───┬──┘     └────┬─────────┘
     │             │
     ▼             ▼
 ┌────────────────────┐
 │ Face Recognition   │
 │ Verification       │
 └────────┬───────────┘
          │
     ┌────▼────┐
     │          │
    ▼           ▼
 ┌──────┐   ┌───────────┐
 │ ALL  │   │ ANY FAILED│
 │ PASS │   │  → ERROR  │
 └──┬───┘   └───────────┘
    │
    ▼
┌──────────────────────┐
│ Mark Attendance      │
│  • Status: Present   │
│  • Store in MongoDB  │
│  • Return Success    │
└──────────────────────┘
```

---

## 📊 Database Schema Relationships

```
┌─────────────────────────────────┐
│         STUDENT COLLECTION      │
├─────────────────────────────────┤
│ _id: ObjectId                   │
│ name: String                    │
│ studentId: String (unique)      │
│ class: String                   │
│ division: String                │
│ gender: String                  │
│ qrCode: String (data URL)       │
│ fingerprint: String (hash)      │
│ faceDescriptor: [Number]        │
│ registeredAt: Date              │
│ createdAt: Date                 │
│ updatedAt: Date                 │
└────────────────────────┬────────┘
                         │
                    ┌────┴────┐
                    │          │
                    │ 1:Many   │
                    │          │
                    ▼          ▼
         ┌──────────────────────────────┐
         │  ATTENDANCE COLLECTION       │
         ├──────────────────────────────┤
         │ _id: ObjectId                │
         │ studentId: ObjectId (ref)    │
         │ date: Date                   │
         │ photoVerified: Boolean       │
         │ fingerprintVerified: Boolean │
         │ qrcodeVerified: Boolean      │
         │ status: String               │
         │ verifiedAt: Date             │
         │ createdAt: Date              │
         │ updatedAt: Date              │
         └──────────────────────────────┘
```

---

## 🔄 Component Architecture (Frontend)

```
┌─────────────────────────────────────────────────────┐
│                   App.jsx                           │
│              (Main Router/Layout)                   │
└──────────────────────┬────────────────────────────┘
                       │
        ┌──────────────┼──────────────┐
        │              │              │
        ▼              ▼              ▼
┌──────────────┐ ┌──────────────┐ ┌──────────────┐
│ Registration │ │  Mark Attend │ │  Dashboard   │
│   Form       │ │  (MFA Flow)  │ │  Analytics   │
└──────┬───────┘ └──────┬───────┘ └──────┬───────┘
       │                │                │
       │         ┌──────┼──────┐         │
       │         │      │      │         │
       │         ▼      ▼      ▼         │
       │    ┌────────┬──────────────┐    │
       │    │ QRScn │ Fingerprint  │    │
       │    │       │ Scanner      │    │
       │    └───────┴──────────────┘    │
       │                │                │
       └────────────────┼────────────────┘
                        │
              ┌─────────▼─────────┐
              │  API Service      │
              │  (axios client)   │
              └───────────────────┘
```

---

## 🛡️ Security Layers

```
┌──────────────────────────────────────────┐
│       FRONTEND SECURITY                  │
├──────────────────────────────────────────┤
│ • Client-side validation                 │
│ • HTTPS ready                            │
│ • Camera permission check                │
│ • Data encoding (Base64)                 │
└──────────────────────────────────────────┘
                  ↓
┌──────────────────────────────────────────┐
│      NETWORK SECURITY                    │
├──────────────────────────────────────────┤
│ • CORS enabled                           │
│ • HTTP/HTTPS support                     │
│ • Request validation                     │
│ • Response standardization               │
└──────────────────────────────────────────┘
                  ↓
┌──────────────────────────────────────────┐
│      BACKEND SECURITY                    │
├──────────────────────────────────────────┤
│ • Input validation                       │
│ • Error handling                         │
│ • SQL injection prevention               │
│ • Rate limiting ready                    │
└──────────────────────────────────────────┘
                  ↓
┌──────────────────────────────────────────┐
│      DATABASE SECURITY                   │
├──────────────────────────────────────────┤
│ • Connection string in .env              │
│ • No raw data exposure                   │
│ • Unique constraints                     │
│ • Data type validation                   │
└──────────────────────────────────────────┘
                  ↓
┌──────────────────────────────────────────┐
│    BIOMETRIC SECURITY                    │
├──────────────────────────────────────────┤
│ • Face descriptors (not images)          │
│ • Fingerprint hashing (SHA-256)          │
│ • Multi-factor requirement               │
│ • Duplicate prevention                   │
└──────────────────────────────────────────┘
```

---

## 🔄 API Request-Response Cycle

```
CLIENT REQUEST
     │
     │ POST /api/students/register
     │ {name, studentId, class, ...}
     ▼
┌─────────────────────────────┐
│   Express Middleware        │
│  • Parse JSON               │
│  • CORS check               │
│  • Body validation          │
└──────────┬──────────────────┘
           │
           ▼
┌─────────────────────────────┐
│   Route Handler             │
│  • Input validation         │
│  • Business logic           │
│  • Database operation       │
└──────────┬──────────────────┘
           │
           ▼
┌─────────────────────────────┐
│   Utility Functions         │
│  • QR code generation       │
│  • Data hashing             │
│  • Verification logic       │
└──────────┬──────────────────┘
           │
           ▼
┌─────────────────────────────┐
│   MongoDB Operation         │
│  • Create/Read/Update       │
│  • Return data              │
└──────────┬──────────────────┘
           │
           ▼
┌─────────────────────────────┐
│   Response Builder          │
│  • Format response          │
│  • Set status code          │
│  • Add headers              │
└──────────┬──────────────────┘
           │
           ▼
SERVER RESPONSE
     │
     │ {success: true, data: {...}}
     │ Status: 201
     ▼
```

---

## 📱 User Journey Maps

### New Student Journey
```
1. REGISTRATION → 2. BIOMETRIC ENROLLMENT → 3. READY FOR ATTENDANCE
   (One-time)        (One-time)
   • Form fill       • Fingerprint          
   • QR Gen          • Face photo           
   • QR Download     • Data stored          
```

### Daily Attendance Journey
```
HOME → QR SCAN → FINGERPRINT → PHOTO → VERIFICATION → RESULT
       (Step 1)    (Step 2)   (Step 3)   (Check all 3)
       
Success Path:   All verified ✓ → Attendance: PRESENT ✓
Failure Path:   Any failed ✗ → Attendance: NOT MARKED ✗
```

### Admin Dashboard Journey
```
DASHBOARD HOME → STATISTICS VIEW → FILTER & ANALYZE → EXPORT/REPORT
                 • Present count
                 • Absent count   
                 • Charts
```

---

## ⚙️ Tech Stack Breakdown

```
FRONTEND STACK
├── React 18 (UI Framework)
├── React Router v6 (Navigation)
├── Axios (HTTP Client)
├── jsQR (QR Scanning)
├── react-webcam (Camera)
├── Recharts (Charts)
└── CSS3 (Styling)

BACKEND STACK
├── Node.js (Runtime)
├── Express.js (Server)
├── MongoDB (Database)
├── Mongoose (ODM)
├── qrcode (QR Generation)
├── cors (CORS Middleware)
├── dotenv (Config)
└── uuid (ID Generation)

TOOLS
├── npm (Package Manager)
├── nodemon (Dev Server)
├── MongoDB Community (Database)
└── REST API (Architecture)
```

---

## 📈 Scalability Architecture

```
Current Setup (1 User):
  1 Backend Server → 1 MongoDB Instance

Scalable Setup (1000+ Users):
  Load Balancer
       ↓
  ┌────┴────┬────────┐
  ▼         ▼        ▼
  Server1  Server2  Server3
    │        │        │
    └────┬───┴────┬──┘
         │        │
    MongoDB Replica Set
    (with backup)
```

---

## 🔔 Notification Flow (Future Enhancement)

```
Attendance Marked
     ↓
┌─────────────────┐
│ Check Settings  │
└────────┬────────┘
         │
    ┌────┼────┐
    │    │    │
    ▼    ▼    ▼
  Email SMS Notification
  ↓     ↓    ↓
 Parent Teacher Student
```

---

## 🎯 Key Integration Points

```
Potential Integrations:
├── Real Fingerprint Scanner API
├── Advanced Face Recognition (AWS Rekognition, Google Cloud Vision)
├── School Management System
├── Email Service (SendGrid, AWS SES)
├── SMS Service (Twilio)
├── Mobile App (React Native)
├── Analytics Platform (Google Analytics)
├── Payment System (For fees/subscriptions)
└── Reporting Tools (Power BI, Tableau)
```

---

## 📊 Performance Monitoring

```
Metrics to Monitor:
├── API Response Time
├── Database Query Time
├── Error Rate
├── Concurrent Users
├── Memory Usage
├── CPU Usage
├── Disk Space
└── Network Bandwidth

Tools to Use:
├── Node.js built-in profiler
├── MongoDB monitoring
├── New Relic / DataDog
├── Sentry for error tracking
└── Google Lighthouse for frontend
```

---

## 🚀 Deployment Architecture

```
Development
├── localhost:3000 (Frontend)
├── localhost:5000 (Backend)
└── localhost:27017 (MongoDB)

Staging
├── staging.example.com (Frontend)
├── api-staging.example.com (Backend)
└── Staging MongoDB

Production
├── www.example.com (Frontend - CDN)
├── api.example.com (Backend - Load Balanced)
└── Production MongoDB (Replicated & Backed up)
```

---

This architecture is designed to be:
- ✅ **Scalable** - Easy to expand
- ✅ **Secure** - Multi-layer protection
- ✅ **Maintainable** - Clear structure
- ✅ **Extensible** - Ready for integrations
- ✅ **Performant** - Optimized for speed
